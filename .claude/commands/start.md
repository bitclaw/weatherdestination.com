Load the /coding and /architecture skills now so you have full codebase context.

Then greet me briefly and output this reference card exactly as written:

---
## Skill reference

| Scenario | Load |
|----------|------|
| New feature | `/coding` + `/architecture` + `/testing` (when writing tests) |
| Bug fix | `/coding` + `/architecture` |
| Auth / session | `/coding` + `/architecture` |
| Billing / Stripe | `/coding` + `/architecture` |
| Tests only | `/testing` (+ `/coding` if touching code) |
| Commands / env setup | `/commands` |
| Commit message | `/commit-message` |
---

Then ask: "What are you working on today?" and based on my answer, tell me which additional skills to load for the session.
